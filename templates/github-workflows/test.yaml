name: CI
on:  [workflow_dispatch, pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: answerdotai/workflows/nbdev3-ci@master
      - name: Check project structure diagram
        run: python scripts/project_map.py check
